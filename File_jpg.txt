import { useState, useEffect, useRef } from "react";

const SUBJECTS = [
  { id:"algebra",   label:"Algebra",    icon:"∑", color:"#00e5ff", bg:"rgba(0,229,255,0.08)",   xp:340 },
  { id:"geometry",  label:"Geometry",   icon:"△", color:"#ffb300", bg:"rgba(255,179,0,0.08)",   xp:210 },
  { id:"physics",   label:"Physics",    icon:"⚡", color:"#e040fb", bg:"rgba(224,64,251,0.08)",  xp:180 },
  { id:"chemistry", label:"Chemistry",  icon:"⚗", color:"#ff4081", bg:"rgba(255,64,129,0.08)",  xp:290 },
  { id:"biology",   label:"Biology",    icon:"🧬", color:"#00e676", bg:"rgba(0,230,118,0.08)",  xp:150 },
  { id:"calculus",  label:"Calculus",   icon:"∫", color:"#448aff", bg:"rgba(68,138,255,0.08)",  xp:120 },
];

const LEVELS = ["Beginner","Intermediate","Advanced"];

const TESTIMONIALS = [
  { name:"Priya S.", grade:"Grade 10", text:"I went from failing Physics to scoring 89% in just 3 weeks. The AI explains things in a way my teacher never could!", avatar:"P", color:"#00e5ff" },
  { name:"Arjun K.", grade:"Grade 11", text:"NOVALearn made Calculus actually fun. The quizzes after each lesson are perfect for exam prep.", avatar:"A", color:"#e040fb" },
  { name:"Zara M.", grade:"Grade 9",  text:"I love how it adapts to my level. It never makes me feel dumb for asking basic questions!", avatar:"Z", color:"#00e676" },
];

const CSS = `
@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Bebas+Neue&family=Fira+Code:wght@400;500&display=swap');

*{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#02040a;
  --s1:#060d1a;
  --s2:#0a1425;
  --s3:#0e1c33;
  --border:rgba(255,255,255,0.07);
  --border2:rgba(255,255,255,0.12);
  --a:#00e5ff;
  --a2:#ffb300;
  --a3:#e040fb;
  --text:#e8f4ff;
  --muted:#4a6380;
  --muted2:#7a96b8;
  --green:#00e676;
  --red:#ff4081;
}
html{scroll-behavior:smooth}
body{background:var(--bg);color:var(--text);font-family:'Plus Jakarta Sans',sans-serif;min-height:100vh;overflow-x:hidden}
.bebas{font-family:'Bebas Neue',sans-serif}
.fira{font-family:'Fira Code',monospace}

::-webkit-scrollbar{width:4px}
::-webkit-scrollbar-track{background:var(--bg)}
::-webkit-scrollbar-thumb{background:rgba(0,229,255,0.3);border-radius:2px}

/* ── ANIMATIONS ── */
@keyframes fadeUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:0.3}}
@keyframes spin{to{transform:rotate(360deg)}}
@keyframes float{0%,100%{transform:translateY(0)}50%{transform:translateY(-14px)}}
@keyframes floatR{0%,100%{transform:translateY(0) rotate(12deg)}50%{transform:translateY(-10px) rotate(12deg)}}
@keyframes glow{0%,100%{box-shadow:0 0 20px rgba(0,229,255,0.3)}50%{box-shadow:0 0 50px rgba(0,229,255,0.7)}}
@keyframes shimmer{0%{background-position:-600px 0}100%{background-position:600px 0}}
@keyframes slideIn{from{opacity:0;transform:translateX(-20px)}to{opacity:1;transform:translateX(0)}}
@keyframes countUp{from{opacity:0;transform:scale(0.5)}to{opacity:1;transform:scale(1)}}
@keyframes borderRotate{0%{background-position:0% 50%}50%{background-position:100% 50%}100%{background-position:0% 50%}}
@keyframes xpFill{from{width:0}to{width:var(--xpw)}}
@keyframes starPop{0%{transform:scale(0) rotate(-30deg);opacity:0}60%{transform:scale(1.3) rotate(5deg);opacity:1}100%{transform:scale(1) rotate(0deg);opacity:1}}
@keyframes particleFloat{0%{transform:translateY(0) translateX(0);opacity:1}100%{transform:translateY(-80px) translateX(var(--px));opacity:0}}

.fu{animation:fadeUp 0.5s ease both}
.fi{animation:fadeIn 0.4s ease both}
.d1{animation-delay:.08s}.d2{animation-delay:.16s}.d3{animation-delay:.24s}.d4{animation-delay:.32s}.d5{animation-delay:.4s}.d6{animation-delay:.48s}
.float{animation:float 4s ease-in-out infinite}
.floatR{animation:floatR 5s ease-in-out infinite}

/* ── NOISE OVERLAY ── */
body::before{
  content:'';position:fixed;inset:0;z-index:0;pointer-events:none;
  background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
  opacity:0.4;
}

/* ── GRID BG ── */
.gridbg{
  background-image:
    linear-gradient(rgba(0,229,255,0.025) 1px,transparent 1px),
    linear-gradient(90deg,rgba(0,229,255,0.025) 1px,transparent 1px);
  background-size:60px 60px;
}

/* ── GLOW ORBS ── */
.orb{position:absolute;border-radius:50%;filter:blur(100px);pointer-events:none;z-index:0}

/* ── NAV ── */
.nav{
  position:fixed;top:0;left:0;right:0;z-index:500;
  display:flex;align-items:center;justify-content:space-between;
  padding:0 52px;height:64px;
  background:rgba(2,4,10,0.75);
  backdrop-filter:blur(28px) saturate(180%);
  border-bottom:1px solid var(--border);
}
.nav-logo{
  display:flex;align-items:center;gap:10px;
  font-family:'Bebas Neue',sans-serif;font-size:26px;letter-spacing:1px;
}
.nav-logo-icon{
  width:34px;height:34px;border-radius:8px;
  background:linear-gradient(135deg,var(--a),#0077ff);
  display:flex;align-items:center;justify-content:center;
  font-size:16px;font-family:'Bebas Neue',sans-serif;color:#02040a;
  box-shadow:0 0 16px rgba(0,229,255,0.5);
}
.nav-logo-text .n1{color:var(--a)}
.nav-logo-text .n2{color:var(--text)}
.nav-links{display:flex;gap:32px}
.nav-link{font-size:13px;font-weight:600;color:var(--muted2);cursor:pointer;transition:color 0.2s;letter-spacing:0.3px}
.nav-link:hover{color:var(--text)}
.nav-btns{display:flex;gap:10px;align-items:center}

/* ── BUTTONS ── */
.btn{font-family:'Plus Jakarta Sans',sans-serif;font-weight:700;border:none;border-radius:10px;cursor:pointer;transition:all 0.2s;position:relative;overflow:hidden}
.btn::after{content:'';position:absolute;inset:0;background:rgba(255,255,255,0);transition:background 0.2s}
.btn:hover::after{background:rgba(255,255,255,0.08)}
.btn-cyan{background:linear-gradient(135deg,#00e5ff,#0077ff);color:#02040a;padding:12px 28px;font-size:14px;box-shadow:0 4px 24px rgba(0,229,255,0.35)}
.btn-cyan:hover{transform:translateY(-2px);box-shadow:0 8px 32px rgba(0,229,255,0.5)}
.btn-cyan:active{transform:translateY(0)}
.btn-cyan:disabled{opacity:0.4;cursor:not-allowed;transform:none;box-shadow:none}
.btn-ghost{background:rgba(255,255,255,0.05);color:var(--text);border:1px solid var(--border2);padding:11px 24px;font-size:14px}
.btn-ghost:hover{background:rgba(255,255,255,0.1);border-color:rgba(255,255,255,0.2);transform:translateY(-2px)}
.btn-sm{padding:8px 18px;font-size:13px}
.btn-xs{padding:6px 14px;font-size:12px;border-radius:7px}

/* ── HERO ── */
.hero{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:80px 52px;position:relative;overflow:hidden}
.hero-inner{max-width:820px;text-align:center;position:relative;z-index:2}
.hero-pill{
  display:inline-flex;align-items:center;gap:8px;
  background:rgba(0,229,255,0.07);border:1px solid rgba(0,229,255,0.2);
  color:var(--a);padding:7px 18px;border-radius:100px;
  font-size:12px;font-weight:700;margin-bottom:40px;
  font-family:'Fira Code',monospace;letter-spacing:0.5px;
}
.pill-dot{width:7px;height:7px;border-radius:50%;background:var(--a);animation:pulse 1.8s infinite;flex-shrink:0}
.hero-h1{font-family:'Bebas Neue',sans-serif;font-size:clamp(72px,12vw,140px);line-height:0.88;letter-spacing:2px;margin-bottom:28px}
.hero-h1 .w1{color:var(--text)}
.hero-h1 .w2{
  background:linear-gradient(90deg,var(--a),var(--a3),var(--a2));
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;
}
.hero-h1 .w3{color:rgba(255,255,255,0.15);-webkit-text-stroke:1px rgba(255,255,255,0.2);-webkit-text-fill-color:transparent}
.hero-sub{font-size:17px;color:var(--muted2);line-height:1.75;max-width:500px;margin:0 auto 48px;font-weight:400}
.hero-cta{display:flex;gap:14px;justify-content:center;flex-wrap:wrap;margin-bottom:60px}
.hero-trust{display:flex;align-items:center;justify-content:center;gap:20px;flex-wrap:wrap}
.trust-avatars{display:flex}
.trust-av{width:32px;height:32px;border-radius:50%;border:2px solid var(--bg);display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;margin-left:-8px}
.trust-av:first-child{margin-left:0}
.trust-text{font-size:13px;color:var(--muted2)}
.trust-text strong{color:var(--text)}
.trust-stars{color:var(--a2);font-size:13px;letter-spacing:2px}

/* ── FLOATING CARDS ── */
.float-card{
  position:absolute;z-index:1;
  background:rgba(10,20,37,0.9);
  border:1px solid var(--border2);
  border-radius:14px;padding:14px 18px;
  backdrop-filter:blur(20px);
  box-shadow:0 20px 60px rgba(0,0,0,0.5);
}
.fc-label{font-size:10px;color:var(--muted);font-family:'Fira Code',monospace;letter-spacing:1px;text-transform:uppercase;margin-bottom:6px}
.fc-value{font-size:20px;font-weight:800;color:var(--a)}
.fc-sub{font-size:11px;color:var(--muted2);margin-top:2px}

/* ── STATS BAND ── */
.statsband{
  display:flex;justify-content:center;align-items:center;
  gap:0;flex-wrap:wrap;
  background:var(--s1);
  border-top:1px solid var(--border);border-bottom:1px solid var(--border);
}
.statitem{
  padding:36px 56px;text-align:center;
  border-right:1px solid var(--border);flex:1;min-width:140px;
}
.statitem:last-child{border-right:none}
.stat-num{font-family:'Bebas Neue',sans-serif;font-size:44px;letter-spacing:1px}
.stat-lbl{font-size:12px;color:var(--muted);font-family:'Fira Code',monospace;letter-spacing:1px;text-transform:uppercase;margin-top:4px}

/* ── SECTION ── */
.sec{padding:110px 52px;max-width:1200px;margin:0 auto;position:relative}
.sec-eyebrow{font-family:'Fira Code',monospace;font-size:11px;color:var(--a);letter-spacing:4px;text-transform:uppercase;margin-bottom:14px;display:flex;align-items:center;gap:10px}
.sec-eyebrow::before{content:'';width:28px;height:1px;background:var(--a)}
.sec-title{font-family:'Bebas Neue',sans-serif;font-size:clamp(42px,6vw,72px);letter-spacing:1px;line-height:0.95;margin-bottom:72px}

/* ── FEATURE CARDS ── */
.feat-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:18px}
.feat-card{
  background:var(--s1);border:1px solid var(--border);
  border-radius:16px;padding:32px;
  transition:all 0.3s;cursor:default;position:relative;overflow:hidden;
}
.feat-card::before{content:'';position:absolute;top:0;left:0;right:0;height:1px;background:linear-gradient(90deg,transparent,var(--a),transparent);opacity:0;transition:opacity 0.3s}
.feat-card:hover{border-color:rgba(0,229,255,0.2);transform:translateY(-5px);background:var(--s2)}
.feat-card:hover::before{opacity:1}
.feat-num{font-family:'Bebas Neue',sans-serif;font-size:48px;color:rgba(0,229,255,0.1);line-height:1;margin-bottom:12px}
.feat-icon{font-size:32px;margin-bottom:14px;display:block}
.feat-title{font-size:17px;font-weight:800;margin-bottom:10px;letter-spacing:-0.2px}
.feat-desc{color:var(--muted2);font-size:14px;line-height:1.65}

/* ── SUBJECTS SHOWCASE ── */
.subj-showcase{display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:16px}
.subj-card{
  border-radius:16px;padding:28px 20px;cursor:pointer;
  transition:all 0.3s;text-align:center;position:relative;overflow:hidden;
  border:1px solid;
}
.subj-card::after{content:'';position:absolute;inset:0;background:linear-gradient(180deg,transparent 40%,rgba(0,0,0,0.4));pointer-events:none}
.subj-card:hover{transform:translateY(-6px) scale(1.02)}
.subj-icon{font-size:38px;margin-bottom:12px;display:block;position:relative;z-index:1}
.subj-name{font-size:16px;font-weight:800;position:relative;z-index:1}
.subj-xp{font-size:12px;font-family:'Fira Code',monospace;margin-top:6px;opacity:0.7;position:relative;z-index:1}
.xp-bar{height:3px;border-radius:2px;margin-top:10px;overflow:hidden;background:rgba(255,255,255,0.1);position:relative;z-index:1}
.xp-fill{height:100%;border-radius:2px;transition:width 1.5s ease}

/* ── TESTIMONIALS ── */
.testi-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:20px}
.testi-card{
  background:var(--s1);border:1px solid var(--border);
  border-radius:16px;padding:28px;position:relative;
}
.testi-quote{font-size:40px;color:var(--a);line-height:1;margin-bottom:14px;font-family:'Bebas Neue',sans-serif;opacity:0.6}
.testi-text{font-size:14px;color:var(--muted2);line-height:1.7;margin-bottom:20px;font-style:italic}
.testi-author{display:flex;align-items:center;gap:12px}
.testi-av{width:40px;height:40px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:15px;color:#02040a;flex-shrink:0}
.testi-name{font-size:14px;font-weight:700}
.testi-grade{font-size:12px;color:var(--muted);font-family:'Fira Code',monospace}
.testi-stars{color:var(--a2);font-size:12px;letter-spacing:2px;margin-top:4px}

/* ── PRICING ── */
.pricing-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:20px}
.price-card{
  background:var(--s1);border:1px solid var(--border);
  border-radius:18px;padding:32px;position:relative;overflow:hidden;
  transition:transform 0.3s;
}
.price-card:hover{transform:translateY(-4px)}
.price-card.featured{
  border-color:rgba(0,229,255,0.4);
  background:linear-gradient(160deg,var(--s2),var(--s1));
}
.price-card.featured::before{
  content:'MOST POPULAR';position:absolute;top:16px;right:16px;
  background:var(--a);color:#02040a;font-size:10px;font-weight:800;
  padding:4px 10px;border-radius:100px;font-family:'Fira Code',monospace;letter-spacing:1px;
}
.price-badge{font-size:13px;font-weight:700;color:var(--muted2);margin-bottom:12px}
.price-amount{font-family:'Bebas Neue',sans-serif;font-size:60px;line-height:1;letter-spacing:-1px;margin-bottom:4px}
.price-amount span{font-size:22px;font-family:'Plus Jakarta Sans',sans-serif;font-weight:600}
.price-period{font-size:13px;color:var(--muted);margin-bottom:24px;font-family:'Fira Code',monospace}
.price-features{display:flex;flex-direction:column;gap:10px;margin-bottom:28px}
.pf{display:flex;align-items:center;gap:10px;font-size:13px;color:var(--muted2)}
.pf::before{content:'✓';color:var(--green);font-weight:800;flex-shrink:0}
.pf.no::before{content:'✕';color:var(--muted)}
.pf.no{opacity:0.4}

/* ── CTA SECTION ── */
.cta-sec{
  margin:0 52px 80px;border-radius:24px;
  background:linear-gradient(135deg,var(--s2),var(--s3));
  border:1px solid rgba(0,229,255,0.15);
  padding:80px 60px;text-align:center;position:relative;overflow:hidden;
}
.cta-sec::before{
  content:'';position:absolute;top:-50%;left:50%;transform:translateX(-50%);
  width:500px;height:500px;border-radius:50%;
  background:radial-gradient(circle,rgba(0,229,255,0.08) 0%,transparent 70%);
  pointer-events:none;
}

/* ── FOOTER ── */
.footer{padding:32px 52px;display:flex;justify-content:space-between;align-items:center;border-top:1px solid var(--border);flex-wrap:wrap;gap:16px}
.footer-logo{font-family:'Bebas Neue',sans-serif;font-size:22px;letter-spacing:1px}
.footer-logo .n1{color:var(--a)}.footer-logo .n2{color:var(--text)}
.footer-links{display:flex;gap:24px}
.footer-link{font-size:13px;color:var(--muted);cursor:pointer;transition:color 0.2s}
.footer-link:hover{color:var(--text)}
.footer-copy{font-size:12px;color:var(--muted);font-family:'Fira Code',monospace}

/* ── DASHBOARD ── */
.dash{display:flex;min-height:100vh}
.sidebar{
  width:240px;background:var(--s1);
  border-right:1px solid var(--border);
  padding:0;display:flex;flex-direction:column;
  flex-shrink:0;position:sticky;top:0;height:100vh;overflow:hidden;
}
.sb-top{padding:20px 16px 12px;border-bottom:1px solid var(--border)}
.sb-logo{font-family:'Bebas Neue',sans-serif;font-size:22px;letter-spacing:1px;display:flex;align-items:center;gap:8px}
.sb-logo-icon{width:28px;height:28px;border-radius:6px;background:linear-gradient(135deg,var(--a),#0077ff);display:flex;align-items:center;justify-content:center;font-size:13px;font-family:'Bebas Neue',sans-serif;color:#02040a}
.sb-logo .n1{color:var(--a)}.sb-logo .n2{color:var(--text)}
.sb-user{padding:12px 16px;border-bottom:1px solid var(--border);display:flex;align-items:center;gap:10px}
.sb-av{width:36px;height:36px;border-radius:10px;background:linear-gradient(135deg,var(--a),#0077ff);display:flex;align-items:center;justify-content:center;font-weight:800;font-size:14px;color:#02040a;flex-shrink:0}
.sb-uname{font-size:13px;font-weight:700}
.sb-ulvl{font-size:11px;color:var(--a);font-family:'Fira Code',monospace}
.sb-xp-wrap{padding:10px 16px 12px;border-bottom:1px solid var(--border)}
.sb-xp-lbl{font-size:10px;color:var(--muted);font-family:'Fira Code',monospace;letter-spacing:1px;text-transform:uppercase;margin-bottom:6px;display:flex;justify-content:space-between}
.sb-xp-bar{height:5px;background:rgba(255,255,255,0.08);border-radius:3px;overflow:hidden}
.sb-xp-fill{height:100%;background:linear-gradient(90deg,var(--a),#0077ff);border-radius:3px;width:68%;transition:width 1s ease;box-shadow:0 0 8px rgba(0,229,255,0.5)}
.sb-nav{flex:1;overflow-y:auto;padding:8px}
.sb-section{font-size:10px;color:var(--muted);font-family:'Fira Code',monospace;letter-spacing:2px;text-transform:uppercase;padding:12px 10px 6px}
.sb-item{display:flex;align-items:center;gap:10px;padding:10px 12px;border-radius:10px;cursor:pointer;font-size:13px;font-weight:600;color:var(--muted2);transition:all 0.2s;border:1px solid transparent;margin-bottom:2px}
.sb-item:hover{color:var(--text);background:rgba(255,255,255,0.05)}
.sb-item.active{color:var(--a);background:rgba(0,229,255,0.08);border-color:rgba(0,229,255,0.15)}
.sb-item .si{font-size:16px;width:20px;text-align:center;flex-shrink:0}
.sb-badge{margin-left:auto;background:var(--a);color:#02040a;font-size:10px;font-weight:800;padding:2px 7px;border-radius:100px;font-family:'Fira Code',monospace}
.sb-bottom{padding:12px 16px;border-top:1px solid var(--border)}

.main{flex:1;overflow-y:auto;background:var(--bg)}
.main-inner{padding:32px 36px;max-width:1100px}

/* ── DASH HEADER ── */
.dh{display:flex;align-items:flex-start;justify-content:space-between;margin-bottom:32px;gap:20px;flex-wrap:wrap}
.dh-left{}
.dh-greeting{font-size:12px;color:var(--muted);font-family:'Fira Code',monospace;letter-spacing:1px;text-transform:uppercase;margin-bottom:6px}
.dh-title{font-family:'Bebas Neue',sans-serif;font-size:36px;letter-spacing:0.5px}
.dh-sub{font-size:13px;color:var(--muted2);margin-top:4px}
.dh-right{display:flex;gap:10px;align-items:center;flex-wrap:wrap}
.streak-chip{display:flex;align-items:center;gap:6px;background:rgba(255,179,0,0.1);border:1px solid rgba(255,179,0,0.25);padding:8px 14px;border-radius:10px;font-size:13px;font-weight:700;color:var(--a2)}

/* ── STAT CARDS ── */
.sc-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(175px,1fr));gap:14px;margin-bottom:28px}
.sc{background:var(--s1);border:1px solid var(--border);border-radius:14px;padding:20px;position:relative;overflow:hidden;transition:border-color 0.3s}
.sc:hover{border-color:var(--border2)}
.sc::before{content:'';position:absolute;top:0;right:0;width:80px;height:80px;border-radius:50%;filter:blur(30px);opacity:0.15}
.sc-lbl{font-size:10px;color:var(--muted);font-family:'Fira Code',monospace;letter-spacing:1px;text-transform:uppercase;margin-bottom:8px}
.sc-val{font-family:'Bebas Neue',sans-serif;font-size:38px;line-height:1;letter-spacing:0.5px}
.sc-change{font-size:11px;margin-top:6px;display:flex;align-items:center;gap:4px}
.sc-change.up{color:var(--green)}.sc-change.down{color:var(--red)}
.sc-pbar{height:3px;background:rgba(255,255,255,0.06);border-radius:2px;margin-top:10px;overflow:hidden}
.sc-pfill{height:100%;border-radius:2px}

/* ── TABS ── */
.tabs{display:flex;gap:2px;background:var(--s1);padding:4px;border-radius:12px;margin-bottom:24px;border:1px solid var(--border)}
.tab{flex:1;padding:10px 8px;text-align:center;border-radius:9px;cursor:pointer;font-size:13px;font-weight:70